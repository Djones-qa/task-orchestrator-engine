{
  "name": "task-orchestrator-engine",
  "version": "1.0.0",
  "private": true,
  "description": "Distributed task scheduling and workflow orchestration platform",
  "workspaces": [
    "packages/shared",
    "services/scheduler-api",
    "services/executor-service",
    "services/webhook-gateway",
    "services/monitor-service"
  ],
  "scripts": {
    "build": "tsc --build",
    "lint": "eslint . --ext .ts",
    "format": "prettier --write \"**/*.ts\"",
    "test": "jest"
  },
  "devDependencies": {
    "@types/jest": "^29.5.11",
    "eslint": "^8.56.0",
    "fast-check": "^3.15.0",
    "jest": "^29.7.0",
    "prettier": "^3.2.4",
    "testcontainers": "^10.4.0",
    "ts-jest": "^29.1.1",
    "typescript": "~5.3.3"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}
