apiVersion: policy/v1
kind: PodDisruptionBudget
metadata:
  name: webhook-gateway-pdb
  namespace: task-orchestrator
  labels:
    app.kubernetes.io/name: webhook-gateway
    app.kubernetes.io/part-of: task-orchestrator-engine
spec:
  minAvailable: 1
  selector:
    matchLabels:
      app.kubernetes.io/name: webhook-gateway
